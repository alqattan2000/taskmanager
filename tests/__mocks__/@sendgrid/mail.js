const chalk = require('chalk')
module.exports = {
    setApiKey() {

    },
    send({text}){
      var t = Date(Date.now())
       // console.log(chalk.green.bold(t + '\n' + text + '\n########## SMS Message Send ############'))
    }
}